
include "common.testo"

test wait_expr_bug_0 {
	my_ubuntu_server {
		start
		wait !"Unexisting text" && "Another nonsense" timeout 5s
	}
}

test wait_expr_bug_1 {
	my_ubuntu_server {
		start
		wait !(!"Unexisting text" && !"Another nonsense") timeout 5s
	}
}

test wait_expr_bug_2 {
	my_ubuntu_server {
		start
		wait !"Lala" && ("Unexisting text" || !(!"Another nonsense")) timeout 5s
	}
}

