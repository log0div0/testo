

machine nana {
	cpus: 1
	ram: 1024Mb
	iso: "${ISO_DIR}/Continent.iso"
	disk_size: 2Gb
}

test herush {
	nana start
	nana wait `select * from
		textlines where text = "Hello world" AND foreground = "blue" AND background = "gray" `
}